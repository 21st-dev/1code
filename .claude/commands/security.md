---
description: Ask the security-reviewer agent to inspect provider, token, renderer, filesystem, or command-execution risk.
argument-hint: "[security-sensitive scope]"
---

@[agent:security-reviewer] @[skill:provider-security-review]

Review this security-sensitive scope:

$ARGUMENTS

Focus on concrete risks, exploit paths, token handling, renderer exposure, logging, and minimal fixes.
